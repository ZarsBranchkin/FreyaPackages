return {
  -- Consider adding a lot more icons to this at some point
  -- All return a function
  -- Function takes an Image GuiObject, and other data
  
  -- (this, string SpriteSheet, string IconName)
  Material = require(script.Material);
}
